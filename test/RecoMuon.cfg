process RecoMuon = {
    
# Event source
    source = PoolSource{
	untracked vstring fileNames ={
	    'file:/data/b/bellan/Produzione/CMSSW_1_1_0_pre3/src/Configuration/ReleaseValidation/data/single_mu_negative.root'
	}
	
	untracked int32 maxEvents = 10
    }

    
# Messages
    service = MessageLogger {
	untracked vstring destinations = { "cout" }
	untracked PSet cout = {
	    untracked bool noLineBreaks = true 
	    untracked string threshold = "INFO"
	}
    }
    
    include "RecoMuon/Configuration/data/RecoMuon.cff"


# Output
    
    module out = PoolOutputModule {
	untracked string fileName ="RecoMuons.root"
    }
    
    path p = {muonreco}
    
    endpath this_is_the_end = {out}
}
